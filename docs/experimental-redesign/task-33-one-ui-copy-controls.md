# Task 33: One UI and Copy Controls

Branch target: `studio/one-ui-copy-controls`

Goal:

- Add copy buttons to copy-ready textboxes.
- Add one browser interface by embedding the Fooocus engine page inside the AI Studio app.
- Keep live generation unchanged.

Runtime boundary:

- Fooocus still runs on `127.0.0.1:7865`.
- AI Studio still runs on `127.0.0.1:7872`.
- The user works from one browser UI at `127.0.0.1:7872` using the embedded Fooocus tab.
